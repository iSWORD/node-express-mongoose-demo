extends ../layouts/default

block main
  center
    h1= title

block content
  ul
    for user in users
      - var name = user.name ? user.name : user.username
      li
        a(href="users/" + user._id)
          h4= name
